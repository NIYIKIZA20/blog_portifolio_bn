export interface Blog {
  id: string;
  title: string;
  name: string;
  description: string;
  profilePhoto?: string;
  created: string;
  updated: string;
  //deteteAt?: string;    i need add this too
} 